  # language setting
language: java
  # jdk tag
jdk:
 - openjdk7

install:
 - pip install awscli

 script:
 - mvn clean package -DskipTests=true
 - ls ./shippable/buildoutput 

after_script:
  - cp -r src ./shippable/buildoutput
 
 
